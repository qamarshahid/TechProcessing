# 🔒 Production Environment Configuration
# Copy to .env.production and set appropriate values

# JWT Configuration (REQUIRED in production)
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long
JWT_EXPIRES_IN=24h

# Authentication (Optional - defaults to demo credentials)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your-secure-admin-password
USER_EMAIL=user@yourdomain.com  
USER_PASSWORD=your-secure-user-password

# Logging Level (production defaults to 'error' only)
LOG_LEVEL=error

# Node Environment
NODE_ENV=production

# API Configuration
PORT=8080

# Database (if using database authentication)
# DATABASE_URL=postgresql://user:password@host:port/database
# DATABASE_SSL=true
